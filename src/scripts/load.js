class Load{
    constructor(){
        this.bar = 0;
        let that = this;
        setTimeout(function(){
            clearInterval(id);
        })
        var id = setInterval();
    }
}

export default Load;